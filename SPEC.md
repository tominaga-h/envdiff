# envdiff — 仕様書

## 1. 目的

`.env` 系ファイル 2 つを **環境変数ごとに** 比較する CLI ツール。

通常の `diff` は行単位で比較するため、コメントや空行、記述順の違いがノイズとして差分に現れ、
本当に知りたい「どの環境変数が違うのか」が埋もれてしまう。envdiff はファイルを環境変数の集合として
解釈し、キー単位で比較する。

### 設計の基準となる一文

> **envdiff は「何が書いてあるか」を比べるツールである。**

判断に迷ったときはこの一文に立ち返る。「実行時に最終的に何がセットされるか」を比べるツールではない
（→ 変数展開を行わない理由、§4.5）。一方で、コメント・空行・クォートの有無といった「書かれ方の違い」は
内容の違いではないため無視する（→ §4.4, §4.6）。

### スコープ外（MVP）

- サブコマンド（単一責務。`envdiff <A> <B>` のみ）
- 3 ファイル以上の比較
- 変数展開（`${FOO}`）
- 標準入力（`-`）からの読み込み
- 差分の重要度判定（「これは設定漏れだから危険」といった主張はしない）
- テーブル出力の色付け

---

## 2. CLI インターフェース

```
envdiff <A> <B> [OPTIONS]
```

### 引数

| 引数 | 説明 |
| --- | --- |
| `<A>` | 比較対象ファイル 1（出力の A 列） |
| `<B>` | 比較対象ファイル 2（出力の B 列） |

`A` と `B` に「基準」「比較対象」といった非対称な意味付けは **しない**。単に第 1・第 2 引数であり、
出力の列順に対応するだけ。どちらを先に渡すかはユーザーの自由で、ツールは意図を推測しない。

### オプション

| オプション | 説明 |
| --- | --- |
| `--all` | 差分のない変数（`same`）も出力に含める |
| `--json` | JSON 形式で出力する（§6） |

`--all`（表示範囲）と `--json`（出力形式）は直交する。組み合わせて使え、互いの意味を変えない。
また `--all` は表示範囲のオプションであり、終了コードの判定基準を変えない（§3）。

### 引数・ファイルに関する扱い

| ケース | 挙動 |
| --- | --- |
| 引数が 2 個でない（0 / 1 / 3 個以上） | usage を stderr に出力し exit 2 |
| ファイルが存在しない・読めない | エラーを stderr に出力し exit 2（メッセージにパスを含める） |
| 同一ファイルを 2 回渡す | 特別扱いしない。差分なしとして exit 0 |
| 空ファイル | 変数 0 個として正常処理。全キーが片方のみとなる。エラーではない |
| ファイル名・拡張子 | 制限しない。`.env` でも `foo.txt` でも渡されたものをパースする |

---

## 3. 終了コード

`diff(1)` の慣習に従う。

| コード | 意味 |
| --- | --- |
| `0` | 差分なし |
| `1` | 差分あり |
| `2` | エラー（パースエラー、ファイル読み込み失敗、引数不正） |

**根拠:** `diff` の代替として使われる以上、`diff` を置き換えても CI が壊れないことに意味がある。
また「差分あり = 1」であることで、`.env.example` と `.env` の乖離を検出する CI ジョブが
`envdiff .env.example .env` の一行で書ける。

**エラーを 1 ではなく 2 に割り当てる根拠:** 1 にすると「正常に動作して差分を見つけた」と
「ファイルを読めなかった」が呼び出し側から区別できない。パースエラーなのにログに「差分あり」と出ると、
担当者は存在しない差分を探すことになる。

### 「差分あり」の定義

`only_in_a` / `only_in_b` / `changed` の **いずれかが 1 件以上** 存在すれば差分ありとする。

**根拠:** 「`.env.example` にあって `.env` にないものだけを失敗にしたい」といったニーズは実在するが、
ツールが差分の重要度を決め始めると単一責務から外れる。選り好みが必要な場合は `--json` を `jq` に通す
（§6）。この逃し弁があるからこそ、本体にフラグを増やさずに済む。

### 警告と終了コード

重複キー警告（§4.7）は終了コードに影響しない。警告は警告であって失敗ではない。

---

## 4. パース仕様

`.env` に正式な仕様は存在せず、実装ごとに方言がある。envdiff は **独自の最小仕様** を以下に定め、
これに従う。既存の dotenv クレートには委譲しない。

**根拠:** dotenv クレートは「環境変数を読み込んで実行時に使う」ためのもので、値を返したら終わりであり、
行番号や重複の情報を捨てる。envdiff が必要とするのはまさにその捨てられる情報である。

### 4.1 基本形

- `KEY=VALUE` を基本形とする
- `=` の **最初の出現** で分割する（値に `=` を含められる）
- キーの前後、`=` の前後の空白はトリムする
- 空行・空白のみの行は無視する

### 4.2 `export` プレフィックス

`export KEY=VALUE` の `export` は許容し、剥がして `KEY=VALUE` として扱う（実運用で頻出のため）。

### 4.3 コメント

- `#` から行末までをコメントとして無視する
- ただし **クォート内の `#` はコメントではない**

### 4.4 クォート

値を囲む `'...'` および `"..."` は **剥がす**。クォートは値の入れ物であって値ではない。

したがって `PORT=3000` と `PORT="3000"` は **`same`** となる。

**根拠:** dotenv がこのファイルを読み込めば両者は等しく `PORT=3000` という環境変数になる。
クォートの有無は「書かれ方の違い」であって「書かれている内容の違い」ではない（§1 の基準）。
これを `changed` と報告するのは、ユーザーが除きたがっているノイズそのものである。

エスケープの解釈:

| クォート | 挙動 |
| --- | --- |
| `"..."` | `\n` `\r` `\t` `\\` `\"` のみエスケープとして解釈する |
| `'...'` | 一切解釈しない（シェルと同じ規則） |

#### 「値を囲む」の定義

クォートを剥がすのは、値の **先頭** がクォートで始まり、そのクォートが値の **末尾** で
閉じている場合に限る。

| 例 | 判定 | 値 |
| --- | --- | --- |
| `KEY="foo"` | 剥がす | `foo` |
| `KEY=foo"bar"` | **素通し** | `foo"bar"` |
| `KEY={"k": "v"}` | **素通し** | `{"k": "v"}` |
| `KEY="foo"bar` | **エラー** | — |

**先頭が非クォートなら素通しする根拠:** 先頭が `f` や `{` である以上、そのクォートは値を
囲んでいない。囲んでいないクォートは §4.4 の対象外であり、`${` を単なる文字として扱う
（§4.5）のと同じく、単なる文字として値に含める。これをエラーにすると
`JSON_FRAGMENT={"k": "v"}` や `MESSAGE=say "hello"` のような、dotenv が問題なく読める
ファイルが比較できなくなる。**dotenv が読めるファイルは envdiff も読めるべき**である
（§4.6・§4.7 と同じ原則）。

**先頭がクォートで閉じた後に文字が続く場合をエラーにする根拠:** `KEY="foo"bar` は、
先頭がクォートであるため読者は「値を囲んでいる」と読むが、実際には囲めていない。
値が `foo` なのか `foobar` なのかは dotenv 実装ごとに割れる。ツールが黙ってどちらかに
決めれば、§4.8 が禁じる「読めなかったものを差分として報告する」ことになる。
`foo"bar"` にこの曖昧さはない（先頭が非クォートである以上、囲むクォートとは読めない）。

> 例: `error: .env:3: unexpected characters after closing quote`

### 4.5 変数展開

`${FOO}` などの変数展開は **行わない**。`${` は単なる文字であり、何の特別扱いもしない。

したがって `API_URL=${BASE_URL}/api/v1` と `API_URL=https://example.com/api/v1` は **`changed`** となる。

**根拠:**

1. envdiff は「何が書いてあるか」を比べるツールである（§1）。「最終的に何がセットされるか」を
   比べるのは別のツールの仕事。
2. 展開はツールを実行環境に依存させる。`${FOO}` の `FOO` がファイル内にないとき、シェル環境から
   取るのか空にするのかエラーにするのか、どれを選んでも「実行環境によって差分の結果が変わる」。
   CI と手元で違う答えを出す比較ツールは信用されない。
3. 展開は情報を捨てる。上の例は「片方はリファクタされ、片方はハードコードのまま」という、
   人間が知るべき差分である。展開するとこれを `same` として握り潰す。

`--expand` のようなオプトインも設けない（オプションであっても環境依存の結果を返す口を持つことになるため）。

### 4.6 複数行の値

クォートが閉じないまま行が終わった場合、**閉じるまで次行以降を値として読み続ける**。`"` と `'` の
両方に同じ規則を適用する。

```
PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
MIIEpAIBAAKCAQEA...
-----END RSA PRIVATE KEY-----"
```

**根拠:** Rails の `master.key`、Firebase のサービスアカウント、GCP 認証情報などで実在する形式。
これをサポートしないと、**完全に正当な `.env` が envdiff では一切比較できない**。しかもエラーは
「7 行目が壊れている」と出るが、7 行目は壊れていない（ツールが読めないだけ）。

ファイル末尾までクォートが閉じなかった場合はパースエラー（exit 2）。
エラーメッセージは **クォートの開始行** を指すこと。

> 例: `error: .env:12: quote opened here is never closed (reached end of file)`

「EOF で閉じていません」だけでは、ユーザーは 200 行のファイルのどこが原因か探すことになる。

### 4.7 重複キー

同一ファイル内に同じキーが複数回現れた場合、**後勝ち**で値を採用し、**stderr に警告を出す**。
比較は正常に継続し、終了コードには影響しない。

```
warning: .env:14: duplicate key 'DB_HOST' (overrides value from line 9)
```

**根拠:** 重複キーはほぼ確実にバグである（書いた人は上の行が効くと思っている）。黙って後勝ちで
処理すると、envdiff が「差分なし」と報告しながら時限爆弾を見逃す。コメントや空行を無視するのは
「意味のないノイズだから」であって、重複キーは「意味のある異常」であり、同じ扱いにはできない。
一方でパースエラーにもしない — dotenv が読めるファイルは envdiff も読めるべきであり（§4.6 と同じ原則）、
`.env` 側の重複のせいで比較全体が失敗すると、ユーザーは本当に知りたかった差分にたどり着けない。

stdout（機械が読む結果）と stderr（人間が読む注意）を分けることで、パイプにも影響しない。

### 4.8 パースエラー

上記のいずれにも当てはまらない行に出会った場合、**その時点でエラー終了する**（exit 2）。
行を読み飛ばして処理を続行してはならない。

**根拠:** 黙ってスキップすると「変数が片方にない」と誤報告し、ユーザーは実ファイルではなく
envdiff のバグを疑うことになる。比較ツールは、読めなかったものを差分として報告してはいけない。

具体的には、コメント・空行・空白のみの行を除いた行のうち、次をパースエラーとする
（いずれも `export` プレフィックスを剥がした後に判定する）。

| 例 | 判定 | 理由 |
| --- | --- | --- |
| `KEY` | エラー | `=` がなく、束縛が存在しない |
| `=VALUE` | エラー | キーがなく、束縛される先がない |
| `KEY=` | **正常** | 値が空文字の変数として存在する（§5.2） |

**`KEY` を「値が空文字の変数」と解釈しない根拠:** `.env` の行は「キーが値に束縛される」ことを
意味する。`=` がなければ束縛そのものが存在せず、何を意味する行なのか決めようがない。
§5.2 が空文字の変数として挙げるのは `KEY=` と `KEY=""` であり、`KEY` ではない。

---

## 5. 比較仕様

### 5.1 分類

各キーは必ず次の 4 状態のいずれかに分類される。

| status | 意味 |
| --- | --- |
| `changed` | 両方に存在し、値が異なる |
| `only_in_a` | A にのみ存在 |
| `only_in_b` | B にのみ存在 |
| `same` | 両方に存在し、値も同じ |

`same` はデフォルトでは出力しない（差分ツールは差分だけを吐くべきで、100 個中 3 個違うときに
97 行のノイズを読ませない）。`--all` で含める。

**`only_in_a` と `only_in_b` を区別する根拠:** `.env.example` と `.env` を比べる典型ケースで、
この 2 つは意味がまったく違う。example にあって手元にない = 設定漏れ、手元にあって example にない =
書き忘れか不要な残骸。一括りにすると、このツールの一番の使いどころで判断材料を潰すことになる。

### 5.2 空文字の扱い

`KEY=` および `KEY=""` は「値が空文字の変数」として **存在する**。「未設定」という第 5 の状態は設けない。

したがって `.env.example` の `API_KEY=` と `.env` の `API_KEY=sk-xxx` は **`changed`** となる。
これは「プレースホルダが埋まった」という事実がそのまま差分になったものであり、正しい報告である。
ツールが「未設定っぽい」と忖度する必要はない。

「値が空文字で存在する」と「キーが存在しない」の区別は、出力上も保持される（§6.1 の `-`、§6.2 の `null`）。

### 5.3 出力順序

キーは **ファイルに書かれている順**（行番号の昇順）に並べる。アルファベット順にはしない。

A と B の行番号は独立した座標系にあるため、次の規則で 1 つの順序に畳む。

1. **A に存在するキー**（`changed` / `same` / `only_in_a`）を、**A の行番号**の昇順に並べる
2. **A に存在しないキー**（`only_in_b`）を、**B の行番号**の昇順に、1 の後ろに続ける

**行番号順にする根拠:** ユーザーは差分を見た後、実ファイルを開いて直す。書かれている順に並んでいれば
出力とファイルを上から突き合わせられる。アルファベット順は「ファイルのどこを見ればいいか」の情報を捨てる。

**A の行番号と B の行番号を混ぜない根拠:** 「A の 5 行目」と「B の 10 行目」を比較して順序を決めることに
意味はない。B の先頭にコメントを数行足しただけで、`only_in_b` のキーが無関係な位置へ動く
（＝ファイルの内容を何も変えていないのに出力の並びが変わる）。また混ぜた順序は A とも B とも一致せず、
突き合わせという目的そのものを失う。上記の規則なら、前半は A を、後半は B をそのまま追える。

**行番号の定義:**

| ケース | 採る行番号 | 理由 |
| --- | --- | --- |
| 重複キー（§4.7） | **後勝ちした行** | 採用される値がその行のもの。値と行番号が食い違うと、出力の行番号を頼りにファイルを開いたユーザーが別の値を見ることになる |
| 複数行の値（§4.6） | **開始行**（`KEY=` の行） | ユーザーがその変数を探すときに開く行 |

---

## 6. 出力仕様

### 6.1 デフォルト出力（テーブル）

`comfy-table` を用いてテーブル形式で出力する。差分がない場合は **何も出力しない**（exit 0 のみ）。

ただし `--all` が指定されている場合は、差分がなくても `same` の行を出力する（exit は 0 のまま）。
`--all` は「`same` も出力に含める」オプションであり（§2）、差分の有無で意味を変えない。

```
KEY       .env (A)  .env.example (B)  STATUS
PORT      3000      8000              changed
DEBUG     -         true              only in B
APP_ENV   1         -                 only in A

3 differences (1 changed, 1 only in A, 1 only in B)
A = .env, B = .env.example
```

#### 列

| 列 | 内容 |
| --- | --- |
| `KEY` | 環境変数名 |
| `<Aのファイル名> (A)` | A における値。存在しない場合は `-` |
| `<Bのファイル名> (B)` | B における値。存在しない場合は `-` |
| `STATUS` | `changed` / `only in A` / `only in B` / `same` |

#### `-` の意味

`-` は **キーが存在しない** ことを示す。値が空文字の変数（`KEY=`）は空セルとして表示され、
`-` とは区別される（§5.2）。

#### STATUS の表記

STATUS には `only in A` / `only in B` と表記し、ファイル名を繰り返さない。

**根拠:** ヘッダーで `.env (A)` と宣言済みであり、STATUS で再びファイル名を書くのは冗長。
`.env.production.local` のような長い名前では STATUS 列だけで幅を食う。また `--json` の
`only_in_a` / `only_in_b` と語彙が一致し、ドキュメントとテストの両方で効く。

#### サマリと凡例

テーブルの後に空行を挟み、サマリ行と凡例行を出力する（いずれも stdout）。

```
3 differences (1 changed, 1 only in A, 1 only in B)
A = .env, B = .env.example
```

- サマリの語彙はテーブルの STATUS と一致させる（`only in A` / `only in B`）
- 凡例行は A/B が何を指すかを示す。テーブルの末尾は必ず画面に残るため、ヘッダーがスクロールで
  流れても A/B の意味を解決できる

#### 長い値の扱い

長い値は `comfy-table` の動的幅調整により **折り返す**（切り詰めない）。ターミナル幅に応じて
レイアウトが調整される。折り返しにより行の高さが不揃いになることは許容する。

**根拠:** 切り詰めると「差分があることは分かるが、何が違うかは分からない」出力になる。
`DATABASE_URL` のホスト名だけが違うとき、それはまさに知りたい情報である。また `same` を出さない以上、
テーブルに載るのは実際に違っている数個だけであり、巨大な行が並ぶ最悪ケースは稀。

> **注:** テーブルの見た目の細部（列幅の配分、折り返しの閾値、罫線スタイル）は実装時に調整してよい。
> 固定すべき契約は「1 キー 1 行」「`-` で不在を示す」「STATUS は A/B 表記」「サマリと凡例を出す」まで。

#### 色

MVP では **テーブルに色を付けない**。

**根拠:** STATUS 列には既に `only in B` と書かれており、色は情報を足していない。また赤/緑は
「悪い/良い」を含意し、ツールが差分の重要度を主張し始めることになる（§3 の方針に反する）。
赤緑は色覚特性上も最も区別しにくい組み合わせである。

ただし **stderr の warning / error には色を付ける**。ここは「注意を引く」ことが目的であり、
意味の重複もなく、色が唯一の役割を果たす。

`NO_COLOR` 環境変数が設定されている場合、および出力先が TTY でない場合は色を無効化する。

#### 強調

`changed` 行の A/B の値セルには、**文字単位の差分に基づく太字**を付ける。値のうち
A と B で実際に異なる部分だけが太字になり、共通部分は無加工で出る。

```
DATABASE_URL │ postgres://localhost:5432/db │ postgres://db.prod:5432/db
                          ~~~~~~~~~                      ~~~~~~~
                          （この部分だけが太字）
```

**対象は `changed` 行の値セルのみ。** `only in A` / `only in B` の行には付けない。
片側にキーが存在しない以上、比較対象がなく「値の中のどこが変わったか」が定義できない。
値を丸ごと太字にすると「値の中のどこかが変わった」という誤読を招くが、実際にはキーごと
存在しないのであり、それは STATUS 列が言っている。`same` 行も定義上差分がないため付けない。

**上の「色を付けない」と矛盾しない。** 太字は色ではない。§6.1 が色を却下した 3 つの根拠に
対して、太字は次の関係にある。

| 「色」節の根拠 | 太字での帰結 |
| --- | --- |
| STATUS 列に既に書いてあり、色は情報を足していない | **文字単位の強調は情報を足す。** 「どの部分が」は STATUS 列が持っていない |
| 赤/緑は「悪い/良い」を含意し、ツールが重要度を主張し始める（§3 に反する） | **消える。** 太字に良し悪しの含意はなく、「ここが違う」しか言わない |
| 赤緑は色覚特性上もっとも区別しにくい | **消える。** 太字は色相ではなく、P 型 / D 型 / 全色盲で等しく読める |

すなわち色を却下した根拠は 3 つとも生きたままであり、太字はそのいずれにも抵触しない。
**A と B で強調を区別しない**（両方とも同じ太字）のも同じ理由による。A/B の区別は列の位置と
ヘッダー（`.env (A)` / `.env.example (B)`）が既に担っており、強調に求められるのは
「値のどの部分が変わったか」だけである。2 種類の強調は役割が重複する。

**太字が出ない端末があることを許容する。** SGR 1 の描画は端末依存であり、bold ウェイトを
持たないフォントでは無視されることがある。ただし強調は**付加情報**であり、意味は STATUS 列と
列位置が担っている。太字が出なくても情報は失われず、手掛かりが減るだけである。
（stderr の色に「唯一の役割を果たす」ことを要求したのとは対照的に、ここでは色や太字が
唯一の伝達手段になることはない。）

色と同じく、`NO_COLOR` および出力先が TTY でない場合は太字を無効化する。判定は stdout に対して
行う（stderr の判定とは独立）。

**位置合わせには差分アルゴリズムが必要である。** 先頭から 1 文字ずつ突き合わせる素朴な実装は、
挿入・削除が入った時点で破綻する。`postgres://localhost:5432/db` と `postgres://db.prod:5432/db`
では `localhost`(9 文字) と `db.prod`(7 文字) の長さが違うため、以降の `:5432/db` は 2 文字
ずれたまま比較され、**両者に同一で存在するのに差分と判定される**。結果として値の大半が太字に
なり、「どこが違うか」を示すという目的が消える。共通部分列の検出には LCS が要る（§7 の `similar`）。

**列幅は ANSI を除いて数える。** `comfy-table` の `custom_styling` feature を有効にする
（既定では無効）。無効のままだとエスケープシーケンスを表示文字として数え、**強調のある行だけ
列幅が広がって罫線が破綻する**。強調は `changed` 行にしか付かないため、崩れるのもその行だけになる。

この経路は通常のテストでは踏めない。テストは非 TTY で走り、そこでは強調が無効化されて
ANSI がセルに入らないためである。したがって回帰テストは ANSI を直接セルに入れ、
「エスケープの有無で列幅が変わらないこと」を確かめる。これは見た目の固定ではなく
（§8.4 の禁止対象ではない）、テーブルが壊れていないことの検証である。

**強調は表示の関心事であり、比較ロジックには持たせない。** §5 の比較結果（`Diff`）は
4 分類までを担い、「値のどの範囲が光るか」は §6.1 の側に閉じる。`--json` が §6.2 で
「整形を一切適用しない」と定めている以上、`Diff` に強調情報を持たせると JSON 側がそれを
無視する構造になり、境界が滲む。

### 6.2 JSON 出力（`--json`）

```json
{
  "files": { "a": ".env", "b": ".env.example" },
  "summary": { "total": 3, "changed": 1, "only_in_a": 1, "only_in_b": 1, "same": 0 },
  "diffs": [
    { "key": "PORT",    "status": "changed",   "a": "3000", "b": "8000" },
    { "key": "DEBUG",   "status": "only_in_b", "a": null,   "b": "true" },
    { "key": "APP_ENV", "status": "only_in_a", "a": "1",    "b": null }
  ]
}
```

#### 設計上の要点

| 項目 | 決定 | 根拠 |
| --- | --- | --- |
| 不在の表現 | `null` | 空文字 `""` と型で区別する（§5.2）。テーブルの `-` に対応 |
| キー名 | `a` / `b` 固定 | ファイル名をキーにすると入力によって JSON の形が変わり、`jq` のクエリが書けない。実ファイル名は `files` に持たせる |
| 整形 | 切り詰め・折り返し・色を一切適用しない | 機械可読とはそういうこと |
| `--all` との組み合わせ | `same` を含める | 表示範囲のオプションであり、出力形式によって挙動を変える理由がない |

`status` の値は `changed` / `only_in_a` / `only_in_b` / `same` の 4 種。

**`--json` を設ける根拠:** テーブル出力は罫線を含むため `grep` / `awk` で扱えない。また §3 で
「ツールは差分の重要度を決めない」と定めたため、「`only_in_a` だけを失敗にしたい」といったニーズは
ユーザー側で解決される必要がある。

```sh
envdiff --json .env.example .env | jq '.diffs[] | select(.status == "only_in_a")'
```

これが書けるなら、本体に `--only-missing` のようなフラグを増やさずに済む。
**`--json` は単一責務を守るための逃し弁である。**

---

## 7. 依存クレート

| クレート | 用途 |
| --- | --- |
| `clap`（derive） | 引数パース |
| `comfy-table` | テーブル出力（`custom_styling` feature が必須 — §6.1 の「強調」を参照） |
| `serde` + `serde_json` | `--json` |
| `anyhow` | エラーハンドリング |
| `owo-colors` | stderr の warning / error の色付け、テーブルの太字（§6.1） |
| `similar` | `changed` 行の値の文字単位差分（§6.1 の「強調」） |

**`owo-colors` を選ぶ根拠:** `NO_COLOR` と TTY の判定を含む挙動が `if_supports_color(Stream::Stderr, ..)`
で完結し、判定ロジックを自前で持たずに済む。`colored` は同等の機能を持つがグローバル状態で色の可否を
管理するため、stdout と stderr で独立に判定する用途（stderr は色、stdout は太字、しかも判定は別）と
噛み合わない。

**`similar` を選ぶ根拠:** `TextDiff::from_chars` が文字粒度の Myers 差分を返し、
`iter_all_changes()` が `Equal` / `Delete` / `Insert` のタグ付きで列挙する。§6.1 が要求する
位置合わせ（LCS）がこれで足り、**トークナイズの設計判断が発生しない** — 何で区切るかを
決める必要がなく、文字を見るため。値を空白で区切る実装は `DATABASE_URL` のような区切りのない値で
巨大なトークン 1 個になり、強調が値全体に広がって無意味になる。

`.env` のパーサは自前で実装する（§4 の根拠を参照）。

---

## 8. テスト戦略

### 8.1 パーサのユニットテスト（本体）

パーサこそがこのツールの価値のすべてである。**§4 の各仕様項目が、そのままテスト名になる** 状態を目指す。

- `export` プレフィックスを剥がす（§4.2）
- `=` の最初の出現で分割する / 値に `=` を含められる（§4.1）
- クォート内の `#` はコメントではない（§4.3）
- `"..."` は `\n` `\r` `\t` `\\` `\"` のみ解釈する（§4.4）
- `'...'` はエスケープを解釈しない（§4.4）
- `PORT=3000` と `PORT="3000"` が等価になる（§4.4）
- `${FOO}` を展開しない（§4.5）
- 複数行の値を読める（§4.6）
- EOF でクォートが閉じない → エラー、かつ **開始行** を指す（§4.6）
- `KEY=` は空文字の変数として存在する（§5.2）
- 重複キーは後勝ち + 警告（§4.7）
- 不正な行 → パースエラーで即停止（§4.8）

### 8.2 比較ロジックのユニットテスト

4 分類（§5.1）が正しく出ること。パース済みの構造体を入力とするため、ファイル I/O は不要。

### 8.3 CLI 統合テスト（`assert_cmd` + `tempfile`）

検証するのは **契約** のみ。

- 終了コード 0 / 1 / 2 の 3 経路（§3）
- `--json` が §6.2 のスキーマで出力されること
  - 文字列比較ではなく `serde_json::Value` にパースして構造で検証する（キー順に依存しないため）
- warning が stdout ではなく stderr に出ること（§4.7）

### 8.4 意図的にやらないこと

**テーブル出力の文字列一致テスト。**

**根拠:** テーブルの見た目は実装時・運用時に調整する前提である（§6.1 の注）。ここをテストで固めると、
表示を調整するたびにテストが赤くなり、テストがリファクタの足枷になる。

> `--json` の形は固定（機械が読むので契約）、テーブルの見た目は自由（人間が読むので調整の余地を残す）。
> **この非対称は意図的である。**

**強調（§6.1）についても、太字が付いた出力の文字列一致テストは書かない。** ただし
**どの範囲を強調するかを決める処理はテストする。** この 2 つは別物である。

| 対象 | テストするか | 理由 |
| --- | --- | --- |
| 「A/B の値 → 強調する範囲」を返す純粋関数 | **する** | 見た目ではなくロジック。回帰しても出力を見ないと気付けない |
| その範囲に ANSI を被せてセル文字列にする処理 | **しない** | まさに上が禁じている「見た目」 |

テストは「`3000` と `8000` なら強調は先頭 1 文字」のように**範囲**を主張し、ANSI エスケープの
有無や太字の出方には触れない。特に `postgres://localhost:5432/db` と `postgres://db.prod:5432/db`
で末尾の `:5432/db` が強調され**ない**ことは、§6.1 が挙げた位置合わせの破綻に対する回帰テストとして
必ず持つ。

CLI 統合テスト（§8.3）は非 TTY で実行されるため、**ANSI が出力に現れないこと**を主張できる。
これはテーブルの見た目ではなく機械可読性の契約であり、書いてよい。

カバレッジの数値目標は設けない（意味のある指標にならないため）。
